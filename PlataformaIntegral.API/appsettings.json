{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*",

  "ConnectionStrings": {
    "PlataformaIntegralDB": "Server=DESKTOP-J0R73JS\\SQLEXPRESS,1433;Database=PlataformaIntegralDB;User Id=adminPI;Password=CREDENCIAL_ELIMINADA;TrustServerCertificate=True;"
  },

  "Jwt": {
    "Key": "CREDENCIAL_ELIMINADA",
    "Issuer": "PlataformaIntegralAPI",
    "Audience": "PlataformaIntegralApps"
  },

  "Minio": {
    // Usamos el nombre del contenedor si está en Docker, o localhost si se ejecuta localmente
    "Endpoint": "localhost:9000",

    // Credenciales del usuario configurado en tu contenedor MinIO
    "AccessKey": "AdminPI",
    "SecretKey": "CREDENCIAL_ELIMINADA",

    // false porque tu contenedor MinIO no usa HTTPS
    "Secure": false,

    // Nombres de buckets personalizados (puedes crear estos desde la API o consola)
    "Buckets": {
      "Videos": "videos",
      "Documents": "documentos",
      "Users": "usuarios",
      "Resources": "otros-recursos",
      "Miniaturas": "miniaturas"
    }
  }
}
